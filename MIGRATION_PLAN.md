# خطة الهجرة — مطحس v4.1 (MIGRATION_PLAN.md)

> **دستور إلزامي موجَّه إلى Claude Code.** اقرأ هذا الملف بالكامل قبل أي إجراء. كل أمر هنا مكتوب بصيغة الأمر المباشر. لا تكتب أي كود تطبيقي الآن — هذه الخطة وهيكلتها فقط. نفّذ المراحل بالترتيب (P0 → P6)، وبعد كل مرحلة نفّذ خطوة التحقق المذكورة قبل الانتقال.

---

## 0. السياق الذي يجب أن تفهمه أولاً

مطحس (Mut'his) v4.1 مساعد مكتبي صوتي **عربي** على Windows 11. تطوّر من مشروع سابق اسمه **SafeGuard (v3.0)** وهو **مجمّد (frozen)**. المشروعان **namespaces معزولة تماماً** ولا يجوز أن تختلطا أبداً (راجع P2 — غير قابل للتفاوض).

### قرارات مثبّتة لا تُناقَش في هذه المرحلة (مصدرها اليوم `AGENTS.md`؛ نُقلت أصلاً عن وثيقة معمارية صارت مؤرشفة وغير مرجعية — DEC-43):

1. **مسار الذكاء = Claude Sonnet** (`claude-sonnet-4-6`) عبر الـ `anthropic` SDK مع SSE streaming. هذا هو المسار **الوحيد** في v1. مسار Gemini Flash والـ router **مؤجَّلان** حتى يثبت المسار الواحد. لا تُدخل Gemini في أي ملف الآن.
2. **LOOK فقط على جهاز المستخدم = حدّ دائم، لا طور.** مطحس يتكلّم ويشير (مستطيل overlay)، ولا ينقر، ولا يكتب، ولا يضغط مفاتيح، ولا يلمس الحافظة (clipboard). أوضاع ASSIST و AUTOPILOT — الآلية الوحيدة التي صُمّمت يوماً لتحريك هذا الحدّ (§12) — **أُلغيت من رؤية المنتج** (DEC-6، 2026-07-14): إلغاء بالتصميم لا تأجيل بالتوقيت. *(تحديث 2026-07-29: تحرّك حدّ منفصل مرّة واحدة — التنفيذ **داخل حاوية يملكها مطحس** بتوقيع دستوري صريح، DEC-3 — وهو لا يمسّ فأرة المستخدم ولا لوحة مفاتيحه ولا حافظته ولا جلسته.)*
3. **حدّ صارم:** لا توجد — ولن تُنشأ — أي أداة `type_text` أو `press_hotkey` أو `real_click` أو `set_trust_mode`، لا ككود ولا كـ stub. **لا يوجد شرط زمني يفتح هذا الحدّ**؛ أي طلب لإضافة إحدى هذه الأدوات هو طلب لتغيير هوية المنتج، ويحتاج توقيعاً دستورياً صريحاً من المالك مسجَّلاً في `DECISIONS.md` — كما حصل التنفيذ داخل الحاوية على توقيعه (DEC-3).

> ملاحظة صدق معماري: الملفات الثلاثة "المهاجرة" أصلها كود SafeGuard v3.0 (تظهر مساراتها القديمة `src/safeguard/...` في رؤوسها). نحن **ننقل ونعيد تأهيل** كوداً مفيداً، ولا "نكتشف ملفات مطحس أصيلة". افهم هذا الفرق بدقة: المسموح هو **نسخ نمط بعد تنظيفه**، وليس استيراد حيّ من v3.0.

---

## P0 — تأكيد النسخة الاحتياطية (Backup) قبل أي تعديل

1. لا تعدّل أي سطر قبل التأكد من وجود نسخة احتياطية عبر Git.
2. تأكّد من وجود **commit أولي** يحفظ الحالة الواردة من المالك كما هي.
3. إن لم يوجد أي commit، توقّف فوراً ونبّه المالك: «لا يوجد commit أولي — لن أبدأ قبل حفظ نسخة احتياطية».

**التحقق (P0):**
```bash
git log --oneline
# يجب أن يظهر سطر الـ commit الأولي على الأقل. إن كان السجل فارغاً → توقّف.
```

---

## P1 — التحقق من الأصول الثلاثة المهاجرة وإعادة تأهيلها

الأصول الثلاثة (تنتقل إلى `src/`): **`tts.py`** و **`screen_scanner.py`** فقط في هذه المرحلة.
أمّا **`cursor_control.py`** فلا يدخل `src/` إطلاقاً (راجع P4 — يبقى في `reference/`).

افحص كل ملف، طبّق التعديلات الموثّقة أدناه، ثم تأكّد من نجاح الاستيراد.

### 1.1 — `src/tts.py` (نظام النطق)
- الوصف: ElevenLabs WebSocket كمسار أساسي، مع سلسلة احتياطية محلية (Windows SAPI عبر COM، ثم pyttsx3). عربي، بلا حالة (stateless)، بلا GPU، يعمل خارج أي `gpu_lock`.
- **تعديل إلزامي:** غيّر `DEFAULT_MODEL_ID` من `eleven_turbo_v2_5` إلى نموذج **Flash v2.5** (`eleven_flash_v2_5`).
- **تنظيف:** احذف كل تعليق يشير إلى SafeGuard أو v3.0 أو رموز الأقسام «§» أو `gpu_lock` بصيغته القديمة. أعد صياغة الـ docstring ليصف مطحس v4.1.
- لا تغيّر منطق سلسلة الـ fallback نفسه — هو سليم ومطلوب.

### 1.2 — `src/screen_scanner.py` (قراءة نص الشاشة)
- الوصف: قراءة نص الشاشة عبر UIAutomation (بدون OCR، أسرع من 30ms، صفر VRAM)، producer غير متزامن، ويتضمّن denylist للنوافذ الحساسة (كلمات المرور). أساس مثالي لوضع LOOK.
- **تحذير حرج موثّق:** سجلّ أخطاء المشروع القديم أظهر `[WinError -2147221008] CoInitialize has not been called` عند استدعاء UIA من thread. **يجب** أن تضمن استدعاء `CoInitialize` (أو آلية تهيئة الـ thread الصحيحة في مكتبة `uiautomation`) **قبل أي عملية UIA** داخل `asyncio.to_thread`. هذه أهم نقطة تقنية في الملف — عالجها صراحةً ووثّق ذلك بتعليق يشرح السبب.
- **تنظيف:** احذف مراجع `SafeGuardDetector` و `Qwen verify` و «Saudi banking apps» ورموز «§» من التعليقات. احتفظ بمنطق الـ denylist والـ BFS كما هو.

### 1.3 — التحقق (P1):
```bash
# الاستيراد ينجح بلا أخطاء:
python -c "import src.tts, src.screen_scanner; print('imports OK')"

# لا توجد أي إشارة إلى v3.0 في الملفين المهاجرين إلى src/:
grep -RniE "safeguard|qwen|§|gpu_lock|concierge" src/tts.py src/screen_scanner.py
# النتيجة المطلوبة: لا مخرجات (صفر تطابق).
```
إن ظهر أي تطابق → نظّفه ثم أعد التحقق. لا تنتقل إلى P2 قبل أن يصبح المخرج فارغاً.

---

## P2 — عزل الـ Namespaces (حرج وغير قابل للتفاوض)

اقرأ هذا القسم كقانون مطلق:

> **v3.0 (SafeGuard) و v4.1 (مطحس) معزولان تماماً.** يُمنع منعاً باتاً **استيراد** أو **استدعاء** أو **الإشارة** إلى أي كود من SafeGuard، وعلى وجه الخصوص:
> - الـ Qwen reasoner (`qwen_reasoner`)
> - الـ ASR المحلي (Whisper المحلي)
> - منطق الرؤية المحلية (local-vision / OmniParser / Florence)

أي محاولة دمج أو استيراد عبر الحدّ بين `safeguard` و `muthis` = **خطأ يوقف العمل فوراً** ويستوجب تنبيه المالك قبل المتابعة. النمط المفيد **يُنسخ بعد تنظيفه**، ولا يُستورد حيّاً.

**التحقق (P2):**
```bash
# لا يوجد أي import أو ذكر لوحدات SafeGuard المحظورة في كامل src/:
grep -RniE "import.*(qwen|safeguard)|from.*(qwen|safeguard)|omniparser|florence|faster_whisper" src/
# النتيجة المطلوبة: لا مخرجات.
```

---

## P3 — توثيق هيكل v4.1

أكّد وضع **الملفات الأربعة الجديدة** + **الأصلين المهاجرين** ضمن بنية منطقية. الشجرة المعتمدة:

```
Muthis_v4.1/
├── MIGRATION_PLAN.md          ← هذا الملف (الدستور المؤقت لمرحلة الهجرة)
├── LESSONS.md                 ← دروس مستخلصة (قراءة/إلهام)
├── CLAUDE.md                  ← يُنشأ في P6 (الدستور الدائم لكل جلسة)
├── CONTRIBUTING.md            ← شروط القبول المُلزِمة
├── DECISIONS.md               ← سجل الأحكام الموقَّعة (DEC-N)
│                                (ARCHITECTURE_v4_1.md غير موجود في المستودع ولا في تاريخه، ومؤرشف وغير مرجعي — DEC-43)
│
├── src/
│   └── muthis/
│       ├── __init__.py
│       ├── cloud/
│       │   ├── __init__.py
│       │   ├── protocol.py            ← (جديد) عقد CloudReasoner + الأحداث الثلاثة
│       │   └── claude_agent.py        ← (جديد) wrapper مسار الجودة (Claude، LOOK-only)
│       ├── tts.py                     ← (مهاجر، مُعاد تأهيله) النطق — Flash v2.5
│       └── screen_scanner.py          ← (مهاجر، مُعاد تأهيله) قراءة الشاشة — CoInitialize
│
├── tests/
│   └── cloud/
│       └── test_claude_agent.py       ← (جديد) اختبار الجلسة الوهمية
│
├── reference/                 ← للقراءة والإلهام فقط — يُمنع النسخ إلى src/
│   ├── cursor_control.py      ← محرك نقر ماوس كامل (مجمّد) — الغرض المذكور أُلغي بـ DEC-6؛ والملف غير موجود اليوم (BLOCKING FINDING)
│   ├── asr.py                 ← Whisper محلي (غير مستخدم؛ درس code-switching العربي)
│   ├── verify_vram.py         ← أداة قياس VRAM للنماذج المحلية
│   └── requirements.lock.txt  ← قفل التبعيات (خذ أقسام الصوت/الأدوات فقط)
│
└── _archive/                  ← ما يُستبعد لاحقاً يُؤرشَف هنا، ولا يُحذف نهائياً
```

> «الملفات الأربعة الجديدة» = `protocol.py` + `claude_agent.py` + `test_claude_agent.py` + الـ `CLAUDE.md` الذي يُنشأ في P6. هذه الملفات بُنيت واختُبرت مسبقاً (الاختبارات الأربعة ناجحة) — لا تُعِد كتابتها؛ ضعها في مواضعها أعلاه فقط.

**التحقق (P3):**
```bash
# الشجرة مطابقة، والملفات في مواضعها:
find src tests reference -type f | sort
```

---

## P4 — المواد المرجعية (reference/) و LESSONS.md

اقرأها للإلهام فقط. **يُمنع نسخ أو دمج أي كود منها إلى `src/`.**

- **`reference/cursor_control.py`** — محرك تحريك ونقر ماوس كامل (ctypes عبر user32.dll، DPI-aware). **الغرض المذكور له سابقاً — «أساس مستقبلي لوضع AUTOPILOT» — لاغٍ:** وضع AUTOPILOT أُلغي من رؤية المنتج (DEC-6)، فلا توجد مرحلة يُبنى نحوها. يبقى المنع كما هو ولسبب أقوى: يُمنع منعاً باتاً وضع أي محرك تحكّم إدخال في `src/` أو استيراده أو استدعاؤه — **دائماً، لا «ما دام الطور LOOK فقط»**. *(ملاحظة 2026-07-29، كنس DEC-7: الملف نفسه غير موجود في المستودع — لا في شجرة العمل ولا في تاريخ git كلّه؛ `reference/` تحوي `asr.py` و `verify_vram.py` وملفَّي القفل فقط. التصرّف النهائي بشأنه ينتظر حكم سلطان — راجع BLOCKING FINDING في `DECISIONS.md`. لم يُحذف شيء ولم يُنقل.)*
- **`reference/asr.py`** — Whisper عربي محلي. غير مستخدم (مطحس يعتمد على المسار السحابي). الدرس الوحيد المفيد منه: دفاعات الـ Arabic code-switching.
- **`reference/verify_vram.py`** — أداة قياس VRAM للنماذج المحلية. مرجع فقط.
- **`reference/requirements.lock.txt`** — قفل التبعيات. خذ منه **أقسام الصوت والأدوات (audio/utility)** فقط؛ تجاهل أقسام الرؤية المحلية (local-vision).

**التحقق (P4):**
```bash
# لا شيء من reference/ تسرّب إلى src/ — خاصة cursor_control:
grep -RniE "cursor_control|import.*asr|verify_vram" src/
# النتيجة المطلوبة: لا مخرجات.
```

---

## P5 — الفهم قبل التنفيذ (بوابة إلزامية — توقّف وانتظر)

قبل كتابة أو تعديل **أي سطر كود جديد** بعد هذه المرحلة:

1. اقرأ **الملفات الأربعة الجديدة فقط** (`protocol.py`, `claude_agent.py`, `test_claude_agent.py`, و`AGENTS.md` إن وُجد).
2. اكتب **ملخّص فهمك** لمعمارية v4.1، يغطّي صراحةً: أوضاع Trust الثلاثة (وأن LOOK فقط هو الطور الحالي)، تفعيل PTT (`Ctrl+Shift+Space`)، مسار Claude الوحيد، الـ Overlay المستطيل وحالاته اللونية، و TTS بنموذج Flash v2.5.
3. ثم **توقّف وانتظر** تأكيد المالك الصريح: «**تمام، كمّل**» قبل أي خطوة لاحقة.

> لا تتجاوز هذه البوابة تحت أي ظرف. لا تبدأ بناء `orchestrator.py` أو أي وحدة جديدة قبل التأكيد.

**التحقق (P5):** وجود ملخّص الفهم مكتوباً، ووقوف العمل فعلياً بانتظار رد المالك.

---

## P6 — إنشاء CLAUDE.md (الدستور الدائم)

أنشئ `CLAUDE.md` ليُقرأ في **كل جلسة**. يجب أن يحتوي صراحةً على:

1. **تعريف مطحس ونطاقه:** مساعد مكتبي صوتي عربي على Windows 11، مسار Claude وحيد، طور LOOK فقط حالياً.
2. **حدوده:** يتكلّم ويشير فقط؛ لا نقر/كتابة/مفاتيح/حافظة. ممنوع وجود أدوات التنفيذ (`type_text`/`press_hotkey`/`real_click`/`set_trust_mode`) بأي شكل.
3. **عزل SafeGuard الصارم:** لا استيراد ولا استدعاء عبر حدّ `safeguard`/`muthis` (راجع P2).
4. **قاعدة «أرشِف، لا تحذف نهائياً»:** أي إبعاد لملف يكون إلى `_archive/`، ولا حذف صلب (hard-delete) أبداً.
5. **تعليمة reference للقراءة فقط:** يُمنع نسخ كود `reference/` إلى `src/`، وعلى رأسه `cursor_control.py`.
6. **قائمة الأصول الثلاثة المهاجرة المشروعة** حتى لا يظنّها Claude Code مخلّفات تُحذف:
   - `src/muthis/tts.py` — نظام نطق مشروع (مهاجر ومُعاد تأهيله، Flash v2.5).
   - `src/muthis/screen_scanner.py` — قارئ شاشة مشروع (مهاجر ومُعاد تأهيله، CoInitialize).
   - `reference/cursor_control.py` — أصل مشروع لكنه **مرجعي ومجمّد**، **لا يُحذف ولا يُنقل إلى src/** (قاعدة «أرشِف، لا تحذف» تبقى سارية). وصفُه القديم بأنه «أساس AUTOPILOT المستقبلي» لاغٍ بـ DEC-6؛ ووجودُه اليوم موضوع حكم مفتوح — راجع BLOCKING FINDING في `DECISIONS.md`.

> ملاحظة لـ CLAUDE.md: إن وُجد `AGENTS.md` في الجذر فهو المصدر الأوحد للحقيقة لكل وكلاء الكود، واجعل `CLAUDE.md` يحيل إليه (سطر واحد: «اقرأ AGENTS.md — هو المصدر الأوحد للحقيقة») بدلاً من تكرار المحتوى وإحداث تعارض.

**التحقق (P6):**
```bash
# CLAUDE.md موجود ويذكر العناصر الستة الإلزامية:
grep -ciE "safeguard|_archive|reference|tts|screen_scanner|cursor_control" CLAUDE.md
# يجب أن يكون العدد ≥ 6.
```

---

## قواعد كتابة هذه المرحلة (تلتزم بها يا Claude Code)

- لغة الأوامر عربية، والمصطلحات التقنية إنجليزية كما هي.
- صيغة أمر مباشرة: «افحص…»، «لا تستورد…»، «توقّف وانتظر…».
- **لا تكتب أي كود تطبيقي في مرحلة الخطة** — الهيكلة والخطة فقط.
- التزم بقوانين AGENTS.md: كل وحدة ≤ 300 سطر، مسؤولية واحدة، stub-first، فصل اللغة (واجهة عربية / لوقات وكود إنجليزي)، و`.env` أولاً قبل أي import.
- **مرجع القوانين هو `AGENTS.md` وحده (DEC-43).** عند أي تعارض بين هذه الخطة و`AGENTS.md` — سواء حول **النطاق الحالي** أو حول **القوانين العامة** — فـ`AGENTS.md` هو المرجع، ومعه `DECISIONS.md` للأحكام الموقَّعة و`CONTRIBUTING.md` لشروط القبول المُلزِمة. **القاعدة القديمة التي كانت تُحيل القوانين إلى `ARCHITECTURE_v4_1.md` أُلغيت:** تلك الوثيقة مؤرشفة وغير مرجعية، وهي أقدم من قرار LOOK-only وتحتوي قرارات أُلغيت لاحقاً (DEC-6). وفي كل الأحوال، إن وجدت تعارضاً جوهرياً **توقّف ونبّه المالك** بدل حلّه صامتاً.
