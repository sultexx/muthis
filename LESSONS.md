# دروس هندسية مستفادة — مطحس v4.1 (LESSONS.md)

> ملف قراءة وإلهام. يوثّق **لماذا** اتخذنا قرارات معمارية معينة، مستلهمة من دراسة مشروع **Clicky** (مساعد macOS مفتوح، مكتوب بـ Swift). هذه ليست تعليمات تنفيذ — التعليمات الملزمة مكانها `AGENTS.md`. هنا نشرح السبب حتى لا يُكرَّر خطأ أو يُلغى قرار صحيح بحسن نية لاحقاً.

> تنبيه عزل مهم: Clicky مشروع macOS (Swift / AppKit / ScreenCaptureKit). نحن استلهمنا **القرارات** لا الـ APIs. مطحس على Windows 11 / Python. لا تنسخ تقنيات Clicky حرفياً — انسخ المبدأ خلفها.

---

## الدرس 1 — التسخين المسبق لـ TLS ومشاركة الـ HTTP Client

**ما لاحظناه في Clicky:** كان يعاني من خطأ `-1200 (errSSLPeerHandshakeFail)` متقطّع عند أول طلب يحمل صورة كبيرة، لأن أول طلب كان يدفع تكلفة مصافحة TLS باردة. وعالجه بإطلاق طلب `HEAD` خفيف في الخلفية عند الإقلاع لتسخين اتصال TLS وتخزين تذكرة الجلسة (session ticket).

**درس ثانٍ من Clicky:** اكتشف أن إنشاء `URLSession` جديد لكل جلسة يفسد connection pool في نظام التشغيل ويسبّب أخطاء `"Socket is not connected"` بعد عدة إعادات اتصال سريعة. الحل: **client واحد طويل العمر** يُشارَك عبر كل الطلبات.

**كيف طبّقناه في مطحس:**
- في `claude_agent.py`، الـ `ClaudeAgent` يملك **`httpx.AsyncClient` واحداً طويل العمر**، ويُمرَّر هذا العميل نفسه إلى `AsyncAnthropic`.
- التسخين (`warm_up_tls`) يطلق `HEAD` عبر **نفس العميل** الذي يستخدمه الـ SDK. هذه نقطة جوهرية: لو سخّنّا عميلاً مختلفاً عن الذي يرسل الطلبات الحقيقية، يصبح التسخين مسرحية بلا فائدة — لأن الـ pool المُسخَّن ليس هو الـ pool المُستخدَم.
- التسخين لا يرمي أي استثناء أبداً (`except` صامت) — هو تحسين بحت، وفشله لا يجب أن يُسقط الإقلاع.

**القاعدة المستخلصة:** التسخين والإرسال الفعلي يجب أن يتشاركا نفس الـ connection pool، وإلا فالتسخين بلا معنى.

---

## الدرس 2 — كشف نوع الصورة (Media-type sniffing) من البايتات

**ما لاحظناه في Clicky:** كان يفحص أول بايتات الصورة ليحدد إن كانت PNG أو JPEG، لأن واجهة الـ API ترفض الطلب إذا كان `media_type` المُعلَن لا يطابق التنسيق الفعلي للبايتات. لقطات الشاشة عنده كانت JPEG، بينما الصور الملصوقة من الحافظة كانت PNG.

**كيف طبّقناه في مطحس:**
- دالة `detect_image_media_type` في `claude_agent.py` تفحص التوقيع البايتي:
  - `89 50 4E 47` (`\x89PNG`) → `image/png`.
  - `FF D8 FF` → `image/jpeg`.
  - غير ذلك → افتراض آمن `image/jpeg` مع تسجيل تحذير.
- في حالتنا، لقطات `mss` + Pillow غالباً PNG، لكننا لا نفترض — نفحص. هذا يوفّر ساعة تنقيح لاحقاً عند أول رفض من الـ API بسبب عدم تطابق `media_type`.

**القاعدة المستخلصة:** لا تُعلِن نوع الصورة من امتداد ملف أو من افتراض؛ اكشفه من البايتات قبل بناء الـ payload.

---

## الدرس 3 — التخلي عن تحريك الماوس الفعلي، والاعتماد على المستطيل (Rectangle Overlay)

**ما لاحظناه في Clicky — وهو أهم درس في هذا الملف:** Clicky **لا ينقر ولا يكتب ولا يحرّك ماوس المستخدم إطلاقاً.** كل ما يفعله: يُخرج النموذج وسماً بإحداثيات (`[POINT:x,y]`)، ثم يطير مؤشر أزرق على الشاشة **يشير فقط**. صانع Clicky اتخذ قراراً واعياً بألا يبني التنفيذ الفعلي أصلاً، رغم أنه التطبيق الأوضح.

**لماذا هذا الدرس حاسم لنا:** تحريك ماوس المستخدم أو الكتابة بناءً على إحداثيات من نموذج لغوي هو **أخطر** جزء وأكثره عرضة للكسر (هلوسة الإحداثيات، انحراف DPI، تغيّر الشاشة بين القرار والتنفيذ). وأقلّه استخداماً يومياً مقابل تعقيده.

**كيف طبّقناه في مطحس (طور LOOK-only):**
- مطحس **يتكلّم ويشير فقط**. لا نقر، لا كتابة، لا ضغط مفاتيح، لا حافظة.
- الإشارة تتم برسم **مستطيل (Rectangle Overlay)** حول العنصر، عبر أداة `highlight_target` فقط.
- **فارق جوهري لصالحنا عن Clicky:** Clicky يستخرج الإحداثيات من نصّ الرد عبر تحليل وسوم (regex). مطحس يستخدم **tool calls منظّمة** — أمتن وأقل هشاشة. لا تتراجع أبداً إلى تحليل إحداثيات من نصّ الرد.
- أوضاع ASSIST و AUTOPILOT (التي ستتضمّن تنفيذاً فعلياً) **مصمَّمة على الورق فقط** ولا تدخل النطاق إلا بقرار صريح يفتح مرحلة Trust Modes. ملف `reference/cursor_control.py` (محرك نقر كامل) محفوظ كأساس مستقبلي لتلك المرحلة، **مجمّد** خارج `src/`.

**القاعدة المستخلصة:** ابدأ بالإشارة لا بالتنفيذ. التنفيذ الفعلي يُؤجَّل خلف بوابة صريحة، ويُبنى فقط بعد أن يثبت الأساس البسيط أنه موثوق في الاستخدام اليومي.

---

## كيف دُمجت هذه الدروس كقوانين في AGENTS.md

الدروس أعلاه ليست توصيات اختيارية — حُوّلت إلى **قوانين ملزمة** في `AGENTS.md` (المصدر الأوحد للحقيقة لوكلاء الكود):

| الدرس | تجسيده كقانون في AGENTS.md |
|------|------------------------------|
| 1 — TLS warmup + client مشترك | بند "Shared HTTP client + TLS warmup" تحت Key Architecture Decisions: عميل واحد طويل العمر، والتسخين عبر نفس العميل. |
| 2 — كشف نوع الصورة | بند "Image media-type sniffing": كشف PNG/JPEG من البايتات قبل إعلان `media_type`. |
| 3 — الإشارة لا التنفيذ | فقرة "CURRENT PHASE: LOOK-only" + أول بند في "Do NOT": يُمنع وجود `type_text`/`press_hotkey`/`real_click`/`set_trust_mode` بأي شكل. وبند "Structured tools, not text tags": لا تحليل إحداثيات من نص. |

> عند أي تعارض مستقبلي: `AGENTS.md` هو مرجع النطاق الحالي، و`ARCHITECTURE_v4_1.md` مرجع القوانين العامة. هذا الملف (`LESSONS.md`) للسياق والتفسير فقط — لا يُشتقّ منه تنفيذ مباشر.