# AUTONOMOUS SESSION PLAN — Mut'his v6 Pro (The Pro Teacher Update)

> ## ✅ STATUS: APPROVED — اعتمدها سلطان (2026-07-14)
> الخطة معتمدة رسميًا بأفكارها الأربع وترتيبها. **بوابة المرحلة A فُتحت (2026-07-14)** بتفويض صريح:
> تنفيذ A كاملة ثم **توقف إلزامي** — لا تُفتح B قبل مراجعة سلطان لنتائج القياس في SESSION LOG.
> البوابات بين المراحل تبقى كما هي: كل مرحلة تُفتح بقرار صريح منفصل.

> عبّئ هذا الملف بالكامل قبل تشغيل الجلسة. كل مرحلة مستقلة بشروط قبول واضحة.
> الترتيب متدرّج بالمخاطرة المعمارية: الدقة (قياسية) → الشارات المرقّمة (امتداد مسار قائم) →
> الترجمة الحية (widget جديد + سلك) → التعتيم السينمائي (نافذة ثانية + مجاهيل Win32/Tk).

---

## 0 · CONTEXT (اقرأ هذا أول)

- **المشروع:** مطحس v6 Pro — «المعلم المحترف»: تعليمي، بصري بامتياز، LOOK-only حصرًا، جاهز لإصدار عام.
- **الفرع:** اعمل فقط على `mut-his-v6-pro`. لا تلمس `main` ولا `mut-his-v5-experimental` نهائيًا.
- **البيئة:** `.venv-v5` (Python 3.14). **حارس الانحدار: 333 passed** — أي نقص تحته = انحدار، أوقف.
  (خط v5 القديم كان 329 + 1 skipped؛ الفرق أن numpy 2.5.1 ثُبّتت في `.venv-v5` فصارت اختبارات
  `test_generate_earcons.py` الأربعة المتخطاة تعمل وتنجح — موثَّق 2026-07-14.)
- **المصدر الأوحد للمعمار:** `AGENTS.md`. عند أي تعارض بين خطتي وبينه، `AGENTS.md` يفوز في القوانين،
  وأبلغني بدل الحل الصامت.

### الخط الأحمر المطلق — LOOK-only (مشدَّد للإصدار العام)
قرار سلطان 2026-07-14: فكرة ACTION/النقر (Trust Modes) **مستبعدة نهائيًا من رؤية المنتج** لأسباب
أمنية تخص المستخدمين العامّين — لم تعد «مؤجلة» بل ملغاة. مطحس يرى ويشير ويرسم ويشرح فقط.
- ممنوع إضافة `type_text` / `press_hotkey` / `real_click` / `set_trust_mode` — لا في schemas ولا كـ stubs.
- أي مهمة تتطلب كسر LOOK-only → ارفضها ونبّهني.
- ملاحظة توثيقية: `AGENTS.md` و`ARCHITECTURE_v4_1.md` §12 ما زالا يصفان Trust Modes كمرحلة مستقبلية
  مصمَّمة؛ إعادة صياغتهما لإعلان الإلغاء النهائي تحتاج توقيع سلطان الصريح (قاعدة Self-Update ‎#4 تحصر
  تحريك هذه الحدود) — مسجَّلة في DECISIONS المعلّقة، لا تُنفَّذ ضمن مراحل هذه الخطة.

### قواعد المعمار (من AGENTS.md — غير قابلة للتفاوض)
- كل module ≤ 300 سطر، مسؤولية واحدة، importable in isolation. الاقتراب من السقف = استخراج، لا ضغط.
- الـ wrappers في `cloud/` لا تملك lifecycles ولا locks ولا loops (Law 11) — الـ orchestrator يملكها.
- DI seams في كل نقطة تكامل. لا مفاتيح في الكود (`.env` فقط، Law 5.1).
- لا coordinate-parsing من prose — tool calls فقط.
- **تزامن Option A لا يُمسّ:** الرسمة الواحدة المخزّنة تُطبَّق لحظة speak، الـ auto-hide يُدرَع من لحظة
  الرسم، والصوت يبدأ مع الرسم. كل ميزة بصرية جديدة تركب هذه النقطة — لا تعيد ترتيبها أبدًا.
- **قاطع الرسم لا يُمسّ:** رسمة واحدة لكل دور عبر البوابة الموحّدة (`HighlightGate` + `tool_choice="none"`).
- Conventional Commits (`feat:`/`fix:`/`docs:`/`chore:`)، رسالة تشرح الـ why. كل ميزة يقابلها اختبار.
- انفصال اللغات (Law §17.5): كل سطح يراه المستخدم عربي؛ logs/أسماء/commits إنجليزية.

### دروس تاريخية إلزامية (سياق للمراحل)
1. **للمرحلة A — سقف إعادة التحجيم في الـ API:** حدّ الـ1280 الحالي ليس اعتباطيًا؛ واجهة claude-sonnet
   تعيد تحجيم أي صورة تتجاوز **≈1568px على الضلع الأطول أو ≈1.15 ميجابكسل**، وعندها يفترق فضاء
   الإحداثيات الذي يفكر فيه النموذج عن الفضاء الذي نحسب منه `scale_x`/`scale_y` — فيتحسّن «الوضوح»
   وينحرف التأشير معًا. ‏1600×900 = 1.44MP يتجاوز الحدين؛ السقف الآمن لشاشة 16:9 هو ~1400
   ‏(1400×787 ≈ 1.10MP). لذلك A قياسية ثلاثية النقاط، لا اعتماد أعمى لـ1600.
2. **للمرحلة D — درس الهالة المحذوفة:** هالة المؤشر حُذفت في v4.1 قرارًا نهائيًا («لا عنصر جديد حول
   المؤشر المنزلق»). التعتيم السينمائي **ليس** عودة لها: هو طبقة حول **الهدف** لا حول المؤشر، وغرضه
   عكسي (إسكات ما حول الهدف بدل إضافة ضجيج حوله). التمييز موثَّق هنا كي لا يُخلَط بينهما.
3. **للمرحلتين C وD — سقفا أسطر فعليان (قيسا 2026-07-14):** `overlay/sidekick_window.py` عند **299**
   و`turn.py` عند **295**. أي إضافة سلك فيهما تستلزم استخراجًا أولًا (نمط `style_env.py`/`voice_out.py`/
   `turn_pass.py` المجرَّب ثلاث مرات في v5) أو تفادي الملف عبر seam — هذا قرار بوابة C0/D0.
4. **من v5 — البثّ الجملي:** `SentenceSplitter` (C1) و`SpeechSession` (C-S) قائمان خلف علم
   `MUTHIS_STREAM_TTS` (default OFF). ترجمة C الحية تركبهما ولا تعيد بناءهما.

---

## 1 · GOAL (هدف واحد واضح)

تحويل مطحس من «مؤشر ناطق» إلى «معلم محترف» عبر أربع قدرات بصرية صرفة — تأشير أدق (قياسًا لا
تخمينًا)، خرائط خطوات مرقّمة، ترجمة نصية حية لما يقوله، وتعتيم سينمائي يركّز الانتباه — دون كسر
LOOK-only ولا تزامن Option A ولا أي سلوك قائم، ومع بقاء الحارس ≥333 وكل الملفات ≤300 سطر.

---

## المرحلة A — دقة التأشير: قياس 1280/1400/1600 (المخاطرة: منخفضة · قياسية · ابدأ هنا)

> وريثة المرحلة D من v5 (التي أُقفلت دون تنفيذ). قياس قبل أي اعتماد — لا التزام مسبق بأي قيمة.

### A · SCOPE
**داخل النطاق:**
- `.env` — قيمة `MUTHIS_VISION_MAX_WIDTH` فقط (الكود يدعمها أصلًا — `vision/downscale.py:46`،
  default ‏1280). التجارب كلها عبر `.env`، صفر تعديل كود في downscale.
- `src/muthis/persona.py` (240 سطرًا — متسع) — سطر توجيه تأشيري واحد + اختباره في `test_persona.py`.
- SESSION LOG — كل القياسات تُدوَّن فيه.

**خارج النطاق صراحةً (لا تلمسه):**
- أي تعديل على scaling math في `downscale.py`/`turn.py`/`shapes.py`.
- أي tool schema، الـ agentic loop، الـ orchestrator.

### A · TASKS
- [ ] **A1 — قياس أساس عند 1280.** وثّق في SESSION LOG دقة التأشير على 3 حالات مرجعية ثابتة
  (زر واجهة قياسي، أيقونة صغيرة في شريط أدوات، عنصر داخل صورة/رسم) بلقطات قبل/بعد، مع تسجيل
  تكلفة tokens والتأخير لكل دور (من `TurnComplete.usage/cost` — البنية تلتقطها أصلًا).
- [ ] **A2 — قياس عند ~1400 (السقف الآمن من إعادة التحجيم).** `MUTHIS_VISION_MAX_WIDTH=1400` عبر
  `.env`، أعد نفس الحالات الثلاث. سجّل فرق الدقة **وفرق التكلفة/التأخير** (الصورة الأكبر أغلى وأبطأ).
- [ ] **A3 — قياس عند 1600 (طلب سلطان الأصلي — فوق حدّي إعادة التحجيم).** أعد القياس. التوقع النظري:
  الـ API يعيد التحجيم فينحرف التأشير رغم وضوح أعلى — لكن القرار للقياس لا للنظرية. سجّل النتيجة
  أيًا كانت.
- [ ] **A4 — توجيه persona تأشيري.** أضف سطرًا واحدًا للـ persona: «صوّب الإحداثيات إلى مركز العنصر
  المستهدف، لا حافته». أعد قياس الحالات الثلاث على أفضل عرض ظهر في A1–A3. اختبار persona جديد
  يثبت وجود السطر.
- [ ] **A5 — الجدول والتوصية.** جدول قبل/بعد كامل في SESSION LOG (دقة × تكلفة × تأخير × كل عرض)
  + توصية صريحة واحدة (اعتماد 1400 أو 1600 أو البقاء على 1280) في DECISIONS FOR SULTAN.
  **لا اعتماد صامت** — تغيير default الـ`.env` الموزَّع يحتاج قرار سلطان.

### A · DEFINITION OF DONE
قياسات الحالات الثلاث موثّقة على العروض الثلاثة · سطر persona مضاف ومختبَر · توصية صريحة تنتظر
قرار سلطان · لا تغيير معتمد بلا قياس · الحارس ≥333 · لا ملف خارج SCOPE تغيّر.

---

## المرحلة B — الشارات المرقّمة: `kind="step"` (المخاطرة: منخفضة-متوسطة)

> البديل التعليمي الشرعي لـACTION الملغاة: «كيف أسوّي X؟» → خريطة طريق بصرية مرقّمة (١، ٢، ٣)
> تُرسم دفعة واحدة، والمستخدم ينفّذ بيده. أقصى ما يبلغه التعليم دون أن يلمس مطحس شيئًا.

### B · SCOPE
**داخل النطاق (كل الملفات دون سقفها بمتسع — قيست 2026-07-14):**
- `src/muthis/shapes.py` (145) — `kind="step"` في نموذج البيانات + التحقق الدفاعي في `parse_shapes_args`.
- `src/muthis/overlay/shapes_widget.py` (120) — فرع رسم الشارة: دائرة نيون + رقم عربي هندي (١٢٣)
  كبير في مركزها + caption chip اختياري.
- `src/muthis/cloud/tool_schemas.py` (108) — إضافة `step` إلى enum الـ`kind` في `draw_shapes` + وصفه.
- `src/muthis/persona.py` (240 + سطر A4) — قاعدة selection: طلب تسلسلي («كيف أعمل...») →
  `draw_shapes` واحدة بعدة شارات step بترتيب التنفيذ، والصوت يشرح بنفس الترتيب.
- الاختبارات: `test_shapes_scaling.py`, `test_shapes_widget.py`, `test_draw_shapes_tool.py`,
  `test_persona.py` + توسيع `scripts/smoke_steps` داخل `smoke_shapes.py` القائم (لا سكربت جديد).

**خارج النطاق صراحةً (لا تلمسه):**
- `highlight_gate.py` / `draw_dispatch.py` / `orchestrator.py` / `turn_pass.py` — **قاطع الرسم لا يُمسّ
  حرفيًا**: «نداء draw_shapes واحد يحمل عدة أشكال» هو النموذج المتوقع أصلًا؛ الشارات مجرد kind جديد
  داخل ذلك النداء الواحد.
- سلوك `highlight_target` وأشكال line/arrow/circle/rectangle القائمة.

**⛔ قيد صدق ملزم:** دقة الشارات region-level كالأشكال القائمة — الـ persona يبقى صريحًا أنها تقريبية،
لا وعد بدقة بكسل.

### B · TASKS
- [ ] **B1 — نموذج البيانات.** `kind="step"` في `shapes.py`: الـ4-tuple هو الـbbox الحاوي (كنمط
  `circle` تمامًا — نفس قاعدة التطويع والقياس، صفر رياضيات جديدة)، والترقيم **بترتيب الظهور في
  القائمة** (أول step في النداء = ١، الثاني = ٢...) — لا حقل جديد في الـ schema (القرار المقترح 1؛
  البديل المرفوض مبدئيًا: حقل رقم صريح — انظر DECISIONS).
  - قبول: `parse_shapes_args` يقبل step ويسقط الفاسد؛ `scale_shape_to_physical` يطابق قاعدة الـbbox
    per-axis (اختبار مرآة `scale_bbox_to_physical` يشمل step)؛ `shapes.py` ≤300.
- [ ] **B2 — الرسم.** فرع step في `shapes_widget.py`: دائرة بتمريرتي التوهج القائمتين (هالة خافتة +
  نواة ساطعة من `OverlayStyle`)، رقم عربي هندي في المركز بخط الـ caption، caption chip اختياري
  تحتها، كل العناصر tag-scoped بـ`SHAPES_TAG` (أبدًا delete-all)، الـkind المجهول يبقى يُتخطى بصمت.
  - قبول: اختبارات FakeCanvas — شارة ترسم دائرة+رقم بالموضع الفيزيائي؛ ثلاث شارات ترقَّم ١٢٣ بترتيب
    القائمة؛ قاعدة الـtag والـ«مجهول يُتخطى» تبقيان خضراوين؛ لون الشارة من `MUTHIS_COLOR_CIRCLE`
    القائم (لا env جديد — إلا بقرار).
- [ ] **B3 — الـ schema والـ persona.** أضف `step` إلى enum `kind` في `tool_schemas.py` مع وصف يوجّه
  النموذج (شارات مرقّمة لتسلسل خطوات واجهة). في `persona.py`: قاعدة selection — «تسلسل كيف-أعمل →
  draw_shapes واحدة بعدة steps بترتيب التنفيذ، ثم اشرح صوتيًا بنفس الترتيب في تمريرة الشرح»؛
  انضباط التمريرتين القائم كما هو (رسم في 1، شرح يبدأ بالمعلومة في 2).
  - قبول: اختبار schema (step ضمن الـ enum، الأدوات الثلاث فقط، لا أداة action)؛ اختبارات persona
    (القاعدة موجودة، الصدق التقريبي موجود، قواعد v5 كلها باقية).
- [ ] **B4 — تنفيذ الحلقة.** اختبار wire في `test_draw_shapes_tool.py`: نداء draw_shapes واحد بثلاث
  شارات step → الثلاث تصل `overlay.draw_shapes` مقيسة per-axis في نداء واحد؛ التمريرة التالية
  `tool_choice="none"`؛ معاند يكرر الرسم ينتهي في تمريرتين برسمة واحدة (القاطع الموحَّد يعمل مع step
  دون أي تعديل عليه).
  - قبول: كل اختبارات القاطع والحلقة القائمة خضراء بلا تعديل عليها.

### B · DEFINITION OF DONE
خريطة مرقّمة كاملة تُرسم من نداء واحد · القاطع والـ dispatch لم يتغيّر فيهما سطر · كل الملفات ≤300 ·
الحارس ≥333 + اختبارات B الجديدة · LOOK-only محفوظ · smoke بصري يعرض ١→٢→٣ على الشاشة الحية.

---

## المرحلة C — الترجمة الحية: شريط ما يقوله مطحس (المخاطرة: متوسطة)

> إتاحة (بيئات صامتة/ضعاف سمع) + تعليم (أسماء UI الإنجليزية تُرى مكتوبة لا مسموعة فقط) + إحساس
> premium. يركب `SentenceSplitter` (v5-C1) عند تفعيل البثّ، ويعرض الرد كاملًا في الوضع المخزَّن.

### C · SCOPE
**داخل النطاق:**
- `src/muthis/overlay/caption_bar.py` — (جديد) view صرف على خيط Tk: chip داكن مستدير أسفل الشاشة
  بنفس لغة `OverlayStyle` النيونية، tag-scoped بـ`CAPTION_TAG`، بلا استيراد tkinter (canvas duck-typed
  → اختبارات بلا شاشة). يعيد استخدام `draw_caption_chip`/`round_rect_points` القائمة في `style.py`.
- `src/muthis/overlay/sidekick_window.py` (**299 — عند السقف**) — أمرا `show_caption`/`clear_caption`
  عبر نفس queue/Tk-thread. **يستلزم استخراج C0 أولًا** (انظر المهمة).
- `src/muthis/voice_out.py` (68 — متسع) — المسار المخزَّن: عرض الرد كاملًا لحظة `speak()` ومسحه بعده.
- `src/muthis/turn_pass.py` (230 — متسع) — المسار المبثوث: `_PassStreamer` يحدّث الشريط جملة-جملة
  لحظة feed (inline — لا مستهلك خلفي).
- `src/muthis/overlay/style.py` (223) + `style_env.py` (117) — `MUTHIS_CAPTIONS` وأي تلوين للشريط.
- الاختبارات: `test_caption_bar.py` (جديد), `test_orchestrator_tts.py`, `test_highlight_sync.py`,
  `tests/conftest.py` (تعقيم `MUTHIS_CAPTIONS`).

**خارج النطاق صراحةً:**
- إعادة ترتيب نقطة تزامن Option A (رسم → درع auto-hide → speak) — الشريط **يستقبل** لحظة speak
  القائمة، لا يغيّرها.
- `speech_stream.py` / `tts_session.py` / سلسلة الـ TTS — تُستهلك كما هي.
- `claude_agent.py` / أي tool schema — الترجمة سطح عرض محلي صرف، النموذج لا يعرف بوجودها.

**⚠️ خطران حقيقيان:**
1. **الخصوصية:** الشريط يعرض **نص المساعد المنطوق فقط** — أبدًا transcript المستخدم، أبدًا tool JSON،
   أبدًا التوجيهات الداخلية «(توجيه داخلي...». نقطة الربط هي `VoiceOut.speak`/`_PassStreamer` — نفس
   حدود الخصوصية القائمة بالضبط، وأي ربط أبكر من speak يكسرها.
2. **الـ ghosting:** لقطة تُلتقط والشريط ظاهر = Claude يقرأ كلامه السابق على الشاشة ويتلوث سياقه.
   المسح يجب أن يكون داخل مسار `hide()` القائم (chokepoint الـ hide-before-capture يغطيه تلقائيًا
   حينها) — اختبار صريح يثبت أن كل التقاط يسبقه مسح للشريط.

### C · TASKS
- [ ] **C0 — بوابة الاستخراج والـ seam (لا كود C قبلها).** قرارا تصميم يُوثَّقان في SESSION LOG:
  (١) **استخراج من `sidekick_window.py` (299):** المرشح المقترح — نقل `dispatch_command` النقي
  واختباراته إلى `overlay/window_commands.py` جديد (هو أصلًا دالة نقية مختبرة بلا Tk — استخراج
  ميكانيكي بنمط `turn_pass.py`)، فيتحرر ~40+ سطرًا لأمري caption (وأمر D لاحقًا). (٢) **سقف
  `turn.py` (295):** لا تضف `show_caption` إلى بروتوكول `Overlay` (يفيض الملف)؛ المقترح: seam
  حقن مستقل في `VoiceOut`/`TurnPass` (‏`show_caption: Callable` بقيمة افتراضية no-op تُسلَّك في
  `main.py` إلى `overlay.show_caption`) — بروتوكول `turn.Overlay` لا يُمسّ وكل fakes القائمة تبقى
  صالحة. إن كشف التنفيذ تعارضًا مع أيٍّ من القرارين → توقف وسجّل في DECISIONS.
  - قبول: القراران موثَّقان + الاستخراج منفَّذ بلا أي تغيير سلوكي (اختبارات dispatch القائمة خضراء
    كما هي) + `sidekick_window.py` نزل بوضوح تحت 300.
- [ ] **C1 — الـ widget.** أنشئ `caption_bar.py`: `show_text(text)` يرسم الـchip أسفل-وسط الشاشة
  (عرض ملفوف لسطرين كحد أقصى — نص أطول يُقصّ بعلامة …)، `clear()` يمسح بالـtag فقط. بلا حالة
  زمنية، بلا مؤقتات — الإظهار والمسح أوامر خارجية صرفة.
  - قبول: اختبارات FakeCanvas — الرسم tag-scoped، المسح لا يلمس بقية الطبقات، النص العربي يصل
    حرفيًا، `caption_bar.py` ≤300.
- [ ] **C2 — السلك المخزَّن (default اليوم).** أمرا القائمة في `sidekick_window.py` (بعد C0) +
  `VoiceOut.speak` يعرض النص كاملًا قبل نداء الـ TTS مباشرة ويمسح في `finally` (النطق فشل أو نجح —
  الشريط لا يعلق). خلف `MUTHIS_CAPTIONS` (default قيد قرار سلطان — التوصية ON، انظر DECISIONS؛
  إلى حين القرار يُبنى default OFF كي يبقى سلوك اليوم حرفيًا).
  - قبول: wire tests — الشريط يظهر لحظة speak (لا لحظة استقبال النص)، يُمسح بعد النطق وعند فشله،
    العلم OFF = صفر نداءات caption، الخصوصية (transcript/tool JSON لا يصلان أبدًا)، وghosting
    (كل التقاط يسبقه مسح — initial + refresh).
- [ ] **C3 — السلك المبثوث.** مع `MUTHIS_STREAM_TTS` ON و`MUTHIS_CAPTIONS` ON: ‏`_PassStreamer`
  يحدّث الشريط بالجملة الحالية لحظة feed لكل جملة (inline await — لا task خلفي)، والمسح عند إغلاق
  الجلسة في نفس مواضع تدهور قرار v5-15 (فشل منتصفي → الشريط يعرض ما يُنطق فعلًا عبر مسار
  الـfallback، لا يتجمد على جملة ميتة).
  - قبول: wire tests — الجمل تتعاقب على الشريط بترتيب النطق؛ تمريرات "auto" المخزَّنة تسلك مسار C2؛
    فشل الجلسة لا يترك الشريط عالقًا؛ حارس anti-streaming القائم أخضر.
- [ ] **C4 — تحقق التزامن والخصوصية الشامل.** شغّل صراحةً: `test_highlight_sync` +
  `test_overlay_autohide` + `test_orchestrator` + `test_orchestrator_tts` + كل اختبارات C الجديدة.
  - قبول: كلها خضراء؛ ترتيب draw→arm→speak لم يتغير في أي سيناريو.

### C · DEFINITION OF DONE
الشريط يعمل بالوضعين (مخزَّن ومبثوث) خلف علم rollback فوري · خصوصية speak محفوظة حرفيًا ·
ghosting مثبت باختبار · `sidekick_window.py` تحت 300 بعد الاستخراج · بروتوكول `turn.Overlay` لم
يُمسّ · Option A كما هو · الحارس ≥333 + اختبارات C.

---

## المرحلة D — التعتيم السينمائي: Spotlight (المخاطرة: الأعلى · آخر شيء · بوابة نموذج أولي)

> أقوى أثر «premium»: عند التأشير تُعتَّم الشاشة (~30%) ما عدا نافذة الهدف — coach-marks حقيقية.
> آخر مرحلة عمدًا: فيها المجهول التقني الوحيد (تراكب alpha+colorkey وz-order في Tk/Win32)، وD0
> نموذج أولي معزول يجيب قبل أي دمج.

### D · SCOPE
**داخل النطاق:**
- `scripts/smoke_focus_dim.py` — (جديد، D0) النموذج الأولي المعزول — **بوابة المرحلة كلها**.
- `src/muthis/overlay/focus_dimmer.py` — (جديد) نافذة Tk Toplevel **ثانية** مستقلة: تعبئة سوداء
  بشفافية `-alpha` (~0.30) + «فتحة» بلون الـ`-transparentcolor` فوق bbox الهدف + نفس ex-styles
  النقر-من-خلال/عدم-التنشيط، تحت نافذة النيون دائمًا في الـ z-order.
- `src/muthis/overlay/sidekick_window.py` (بعد استخراج C0) — سلك إظهار/إخفاء التعتيم داخل مساري
  `show`/`hide` القائمين (نفس queue/Tk-thread).
- `style.py`/`style_env.py` — ‏`MUTHIS_FOCUS_DIM` (**default OFF** — ميزة جديدة حساسة بصريًا) +
  `MUTHIS_FOCUS_ALPHA` (default ‏0.30، مضبوطة بـ`clamp01` القائمة).
- الاختبارات: `test_focus_dimmer.py` (جديد — fakes بلا Tk)، `test_highlight_sync.py`،
  `test_pointer_look_only.py` (يبقى أخضر — لا API فأرة جديد)، `tests/conftest.py` (تعقيم
  `MUTHIS_FOCUS_DIM`).

**خارج النطاق صراحةً:**
- أي تعديل على نافذة النيون القائمة أو الـ canvas المشترك أو `rectangle_widget.py`.
- توقيت auto-hide (7 ثوانٍ من لحظة الرسم) — التعتيم يظهر ويختفي **مع** المستطيل، لا مؤقت خاص به.
- التعتيم مع `draw_shapes` — نسخة v1 تعتّم مع `highlight_target` فقط (نية «حدّد-لي» — التوصية
  المقترحة 3؛ الأشكال التوضيحية تحتاج سياقها الكامل ظاهرًا).

**⚠️ المخاطر الحقيقية الثلاثة:**
1. **تراكب الشفافيتين:** ‏`-alpha` (شفافية النافذة كلها) + `-transparentcolor` (فتحة colorkey) على
   نفس الـ Toplevel — يعمل نظريًا على Windows ‏(LWA_ALPHA + LWA_COLORKEY معًا) لكنه غير مجرَّب في
   بنيتنا. لهذا نافذة **ثانية** أصلًا: لو وُضع alpha على نافذة النيون لخفت النيون نفسه معه.
2. **z-order:** التعتيم يجب أن يبقى تحت النيون وفوق تطبيقات المستخدم، بلا سرقة تركيز، وبلا وميض
   عند الإظهار/الإخفاء.
3. **الـ ghosting:** لقطة والتعتيم ظاهر = يرى Claude شاشة معتّمة ويتوه. الإخفاء يركب مسار `hide()`
   القائم فيغطيه chokepoint الـ hide-before-capture تلقائيًا — واختبار صريح يثبته.

### D · TASKS
- [ ] **D0 — النموذج الأولي المعزول (بوابة المرحلة كلها — لا كود دمج قبله).** `smoke_focus_dim.py`
  مستقل تمامًا عن المشروع: افتح النافذتين على Win11 الحقيقي وأجب كتابةً في SESSION LOG: (١) هل
  alpha+colorkey يتعايشان على نفس الـ Toplevel؟ (٢) هل الفتحة شفافة 100% والنقر يمر خلالها وخلال
  المعتَّم؟ (٣) هل z-order مستقر (تعتيم تحت نيون) بلا وميض؟ (٤) قياس أي أثر أداء ملحوظ.
  **قرار البوابة:** أي فشل → توقف، سجّل في DECISIONS مع الخطة البديلة المسجَّلة سلفًا (أربعة
  مستطيلات معتمة حول الـbbox في نافذة alpha واحدة بلا colorkey — أفقر بصريًا لكنه مضمون)، ولا تنفّذ
  D1–D3 فوق أساس هش.
  - قبول: إجابات موثّقة + قرار بوابة صريح (متابعة / بديل / إلغاء).
- [ ] **D1 — الـ widget/النافذة.** `focus_dimmer.py`: ‏`show_around(bbox)` يرسم التعبئة + الفتحة
  (bbox موسَّع بهامش تنفس صغير ثابت)، ‏`hide()` يخفي، init فاشل → no-op (لا يقتل خيط Tk أبدًا —
  قاعدة المرونة القائمة). بلا استيراد tkinter على مستوى الموديول (lazy كنمط الحزمة).
  - قبول: اختبارات fakes — الفتحة تطابق الـbbox+الهامش، hide يخفي، الفشل no-op، ≤300 سطر.
- [ ] **D2 — السلك.** داخل مسار `show` القائم (لحظة وصول أمر الرسم على خيط Tk، قبل رسم المستطيل)
  يظهر التعتيم إن كان العلم ON وكان الرسم `highlight` (لا `shapes`)؛ ‏`hide()` القائم يخفيه دائمًا —
  فيرثه auto-hide وchokepoint الالتقاط والإغلاق تلقائيًا، **صفر تغيير في التوقيتات**.
  - قبول: wire tests — التعتيم يظهر مع المستطيل ويختفي معه (auto-hide + hide-before-capture +
    الإغلاق)، لا يظهر مع draw_shapes، العلم OFF = صفر نداءات، Claude لا يرى شاشة معتّمة في أي
    التقاط (initial + refresh).
- [ ] **D3 — تحقق التزامن الختامي.** شغّل حزم التزامن الست (نمط v5-C3) + `test_pointer_look_only`.
  - قبول: كلها خضراء؛ Option A (الرسم لحظة speak، auto-hide من لحظة الرسم) لم يُمسّ.

### D · DEFINITION OF DONE
D0 مُجاب وقراره صريح · التعتيم يظهر/يختفي مع المستطيل حصرًا (لا مؤقت خاص) · default OFF ·
ghosting مثبت · نافذة النيون لم تُمسّ · الحارس ≥333 + اختبارات D · LOOK-only محفوظ
(`test_pointer_look_only` أخضر).

---

## SELF-VERIFICATION LOOP (حبل الأمان — إلزامي بعد كل مهمة)

1. شغّل: `PYTHONPATH=src python -m pytest tests -q` (على `.venv-v5`).
2. إذا فشل أي اختبار → صحّح قبل الانتقال. لا تتقدّم فوق اختبار أحمر.
3. تأكد أن العدد بقي **333 passed** (أو أعلى مع اختبارات جديدة). أي نقص = انحدار، أوقف.
4. إذا كسرتَ اختبارًا قديمًا ولم تعرف السبب → تراجع، سجّل في SESSION LOG، انتقل للمهمة التالية.
5. commit بعد كل مهمة ناجحة (Conventional Commit) + تحديث جدول Key Files في `AGENTS.md` لكل ملف
   جديد/مستخرَج (قاعدة Self-Update).

## GUARDRAILS (خطوط حمراء)
- **لا كود تطبيق قبل موافقة سلطان على هذه الخطة** — ثم لا فتح مرحلة قبل إقفال سابقتها (بوابات).
- لا تعدّل أي ملف خارج SCOPE للمرحلة الحالية.
- لا تحذف اختبارًا لتمريره. الأحمر يُصلَّح.
- لا تتجاوز 300 سطر في أي ملف — استخرج موديولًا بدلًا من ذلك (`sidekick_window.py` 299 و`turn.py`
  295: أي سلك فيهما يمر ببوابة C0).
- لا تخمّن عند غموض معماري — سجّله في DECISIONS FOR SULTAN وتخطّاه.
- LOOK-only مطلق: لا أداة action، لا SetCursorPos/mouse_event/SendInput، لا حافظة — في كل المراحل.
- تزامن Option A وقاطع الرسم الموحَّد لا يُمسّان في أي مرحلة.
- المرحلة A: قياس قبل أي اعتماد؛ التوصية لسلطان، لا اعتماد صامت.
- المرحلة B: القاطع والـdispatch خارج النطاق حرفيًا؛ الترقيم بترتيب القائمة ما لم يقرر سلطان غيره.
- المرحلة C: الخصوصية (نص المساعد فقط) والـghosting (مسح قبل كل التقاط) شرطا قبول لا تفاوض فيهما.
- المرحلة D: D0 يسبق أي دمج؛ default OFF؛ التعتيم بلا مؤقت خاص به.

## DECISIONS FOR SULTAN (قرارات تنتظرني)
<!-- Claude Code يكتب هنا أي قرار علّق عنده بدل إيقاف الجلسة. أراجعه لما أرجع. -->

### محسومة (سلطان — 2026-07-14، جولة مقترحات v6)
1. **الأفكار الأربع معتمدة كمراحل A→D بهذا الترتيب** (دقة → شارات → ترجمة → تعتيم)، بترتيب
   المخاطرة المعمارية تصاعديًا.
2. **حذف `screen_scanner.py` اليتيم معتمد ومنفَّذ** (commit `e48b1b6`) — الحارس بقي 333.
3. **Trust Modes ملغاة نهائيًا من رؤية المنتج** (لا مؤجلة) — انظر البند التوثيقي المعلّق أدناه.

### معلّقة (تنتظر قرار سلطان)
- **اعتماد الخطة نفسها:** هذا الملف مسودة — لا كود تطبيق قبل الموافقة الصريحة.
- **default علم `MUTHIS_CAPTIONS` (مرحلة C):** التوصية ON عند الإصدار العام (قيمة ظاهرة للجميع،
  وrollback بمتغير واحد)؛ يُبنى OFF إلى حين القرار.
- **ترقيم الشارات (مرحلة B):** التوصية «بترتيب الظهور في القائمة» (صفر تغيير في بنية الـ schema)؛
  البديل حقل رقم صريح لو أردت سماح النموذج بترقيم غير متسلسل.
- **نطاق التعتيم (مرحلة D):** التوصية `highlight_target` فقط في v1؛ البديل تعميمه على draw_shapes.
- **إعلان إلغاء Trust Modes في الوثائق:** إعادة صياغة فقرات LOOK-only في `AGENTS.md` وبند §12 في
  `ARCHITECTURE_v4_1.md` من «مؤجلة» إلى «ملغاة» — تعديل دستوري يحتاج توقيعك الصريح (Self-Update ‎#4)،
  ويُقترح تنفيذه كمهمة docs مستقلة قبل الإصدار العام.

## SESSION LOG (يكتبه Claude Code أثناء العمل)
<!-- كل مهمة، كل اختبار كُسر وأُصلح، كل موديول استُخرج، كل تراجع. -->

- 2026-07-14 · الفرع `mut-his-v6-pro` أُنشئ من رأس `mut-his-v5-experimental`؛ `plan_v5.md` وُسم
  COMPLETELY FINISHED (commit `1dc924c`).
- 2026-07-14 · خط الأساس مؤكَّد: `pytest -q` → **333 passed** على `.venv-v5` (الفرق عن 329+1skipped
  الموثَّق في v5: numpy 2.5.1 ثُبّتت فصارت اختبارات `test_generate_earcons.py` الأربعة تعمل).
- 2026-07-14 · نظافة ما قبل الإصدار: حُذف `src/muthis/screen_scanner.py` اليتيم (مهاجر قديم، لا
  مستورِد حي، غير موثَّق في Key Files) — commit `e48b1b6`، الحارس 333 ✅.
- 2026-07-14 · قياس أسقف الأسطر الفعلية للملفات التي ستُمسّ: `sidekick_window.py` **299** و`turn.py`
  **295** (→ بوابة C0)، `shapes.py` 145، `shapes_widget.py` 120، `tool_schemas.py` 108،
  `persona.py` 240، `voice_out.py` 68، `turn_pass.py` 230، `style.py` 223، `style_env.py` 117.
- 2026-07-14 · **هذه الخطة صيغت وقُدّمت لسلطان — بانتظار الموافقة. لا كود تطبيق قبلها.**
