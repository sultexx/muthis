# AUTONOMOUS SESSION PLAN — Mut'his v5 Enhancements (REVISED)

> عبّئ هذا الملف بالكامل قبل تشغيل الجلسة. كل مرحلة مستقلة بشروط قبول واضحة.
> الترتيب متدرّج بالمخاطرة: المؤشر (منخفض) → الإسهاب (متوسط) → البثّ (عالٍ، حسّاس) → الدقة (اختياري، قياسي).
> REVISED: قيد لا-هالة في A · تسامح STT وواقعية EXACT_N في B · مهمة C0 وقيد "دور فيه tool_use = buffer كامل" في C · مرحلة D اختيارية.

---

## 0 · CONTEXT (اقرأ هذا أول)

- **المشروع:** مطحس v5 — نسخة تجريبية من v4.1
- **الفرع:** اعمل فقط على `mut-his-v5-experimental`. لا تلمس `main` نهائياً.
- **البيئة:** `.venv-v5` (مفعّلة). حارس الانحدار: 278 اختباراً يجب أن تبقى خضراء (torch-gated tests تُتخطى، هذا طبيعي).
- **المصدر الأوحد للمعمار:** `AGENTS.md`. عند أي تعارض بين خطتي وبينه، `AGENTS.md` يفوز في القوانين، وأبلغني بدل الحل الصامت.

### الخط الأحمر المطلق — LOOK-only
مطحس يرى الشاشة ويشير فقط. لا ينقر، لا يكتب، لا يضغط مفاتيح، لا يلمس الحافظة.
- ممنوع إضافة `type_text` / `press_hotkey` / `real_click` / `set_trust_mode` — لا في schemas ولا كـ stubs.
- أي تحسين هنا يجب أن يبقى ضمن LOOK-only. لو أي مهمة تتطلب كسره → ارفضها ونبّهني.

### قواعد المعمار (من AGENTS.md — غير قابلة للتفاوض)
- كل module ≤ 300 سطر، مسؤولية واحدة.
- الـ wrappers في `cloud/` لا تملك lifecycles ولا locks ولا loops (Law 11) — الـ orchestrator يملكها.
- DI seams في كل نقطة تكامل. لا مفاتيح في الكود (`.env` فقط، Law 5.1).
- لا coordinate-parsing من prose — tool calls فقط.
- Conventional Commits (`feat:`/`fix:`/`test:`)، رسالة تشرح الـ why.
- كل ميزة جديدة يقابلها اختبار.

### درس تاريخي إلزامي (سياق للمرحلة C)
sentence-streaming جُرّب سابقاً (Batch 3) وفشل UX-ياً: تقطيع مسموع بين الجمل، ورُجع عنه إلى buffer-then-speak. السبب الجذري وقتها: المحرّك كان اجمع-ثم-شغّل لكل جملة. اليوم المحرّك ElevenLabs WebSocket يبثّ داخل الجملة — الفكرة قابلة للنجاح **فقط** إذا مرّت كل الجمل عبر اتصال WS واحد دائم (لا اتصال-لكل-جملة). هذا هو الفيصل، ومهمة C0 تتحقق منه قبل أي كود.

---

## 1 · GOAL (هدف واحد واضح)

رفع جودة تجربة مطحس عبر المحاور — مؤشر أحدث يعكس الحالة، تحكّم صوتي في الإسهاب، بثّ صوتي أسرع للردود النصية الصرفة، و(اختيارياً) دقة تأشير أعلى — دون كسر LOOK-only ولا أي سلوك قائم، ومع بقاء الـ278 اختباراً خضراء.

---

## المرحلة A — المؤشر والأشكال (المخاطرة: منخفضة · ابدأ هنا)

### A · SCOPE
**داخل النطاق (ملفات التصميم البصري فقط):**
- `src/muthis/overlay/style.py` — نقطة التخصيص الوحيدة
- `src/muthis/overlay/style_env.py` — (جديد — معتمد 2026-07-13) env-parsing مستخرج من `style.py` احترامًا لسقف الـ300 (الملف كان عند 300 سطر بالضبط)
- `src/muthis/overlay/status_indicator.py` — حالة المؤشر البصرية
- الاختبارات المقابلة: `test_overlay_style.py`, `test_status_indicator.py`
- ~~`src/muthis/overlay/pointer_widget.py` — شكل المؤشر المنزلق~~ (خرج من النطاق بإلغاء A1)

**خارج النطاق صراحةً (لا تلمسه):**
- منطق التزامن صوت↔مؤشر في `orchestrator.py` (المرحلة C تلمسه، ليس الآن)
- `rectangle_widget.py` سلوك الـ highlight الأساسي
- أي منطق إحداثيات أو scaling

**⛔ قيد تصميمي ملزم — لا عودة للهالة:** هالة/حلقة حول المؤشر حُذفت في v4.1 قراراً نهائياً (كانت تزاحم المحتوى فوق الكود). عكس الحالة يكون بتلوين **جسم المؤشر نفسه** (لون النواة أو شدّة توهّجه الحالي) — لا عنصر جديد حوله، ولا توهّج متضخّم يعيد أثر الهالة باسم آخر. أي مهمة تتجه لذلك → توقف وسجّل في DECISIONS.

### A · TASKS
- ⛔ **A1 — أُلغيت (قرار سلطان 2026-07-13، الخيار ج).** ~~ربط حالة الجلسة بلون المؤشر المنزلق: أضف في `style.py` تدرّج ألوان/توهّج لكل حالة (listening/thinking/speaking) بحيث المؤشر المنزلق نفسه يعكس الحالة.~~
  - سبب الإلغاء: التنفيذ الفعلي يستلزم سلكًا خارج النطاق (فرع `set_state` في `dispatch_command` بـ`sidekick_window.py` — 299 سطرًا) وإعادة رسم للمؤشر الساكن عند كل انتقال حالة، والمكسب البصري ضئيل (المؤشر لا يظهر إلا لحظة بدء الكلام). النقطة الركنية كافية لتمثيل الحالة.
- [x] **A2 — نبض أنعم للحالة (مقياس معتمد 2026-07-13).** حسّن الـ pulse في `status_indicator.py` كمّيًا: `_PULSE_PERIOD_MS` من 1400 إلى 2000، و`DOT_PULSE_AMP` من 3 إلى 2px، مع بقاء الـ self-rescheduling loop (لا blocking sleep).
  - الملف: `status_indicator.py` (+ اختبار كمّي في `test_status_indicator.py`)
  - قبول: لا blocking sleep/while جديد؛ اختبار كمّي جديد يعاين نصف قطر النقطة عبر FakeCanvas (ذروة النفَس = 7+2px عند منتصف الدورة، الدورة تُغلق بعد 2000ms، فرق نصف القطر بين إطارين متتاليين < 0.2px)؛ اختبارات الـ pulse القائمة تبقى خضراء.
- [x] **A3 — توهّج أنعم (مقياس معتمد 2026-07-13).** خفض `glow_intensity` الافتراضي 0.45 → 0.35 في `style.py` (هالة أخفت لكل الأشكال المرسومة)، بعد استخراج env-parsing إلى `style_env.py` (شرط سقف الـ300). جزء «خط caption أوضح» بلا مقياس معتمد → معلّق في DECISIONS، لا يُنفَّذ.
  - الملفات: `style.py` + `style_env.py` (الجديد) + تحديث موثَّق لاختبارات الـ defaults
  - قبول: `OverlayStyle()` يبقى deterministic (defaults ثابتة)؛ malformed env يبقى يحذّر ويرجع للـ default؛ تحديث قيم 0.45 المثبَّتة في `test_overlay_style.py` يُسجَّل في SESSION LOG (تغيير default موثَّق، لا إسكات اختبار).

### A · DEFINITION OF DONE
مهام A المتبقية (استخراج `style_env.py`، A2، A3) مكتملة · A1 ملغاة وموثَّقة · الـ278 اختباراً خضراء (أو أعلى) · لا شيء خارج SCOPE تغيّر · لا عنصر جديد حول المؤشر · LOOK-only محفوظ · commit واحد لكل مهمة.

---

## المرحلة B — التحكم الصوتي في الإسهاب (المخاطرة: متوسطة)

### B · SCOPE
**داخل النطاق (محدَّث بقرارات 2026-07-13):**
- `src/muthis/verbosity.py` — (جديد) آلة حالة الإسهاب + التوجيهات الداخلية + الكشف
- `src/muthis/voice_out.py` — (جديد، استخراج معتمد) `_speak` + `_refuse_for_budget` خارج الـ orchestrator
- `src/muthis/persona.py` — سياسة الإسهاب + سطر طاعة «التوجيه الداخلي» الوارد في رسائل المستخدم
- `src/muthis/orchestrator.py` — ربط begin_turn/end_turn فقط (بعد الاستخراج)
- `tests/test_verbosity.py` (جديد), `tests/test_persona.py`, `tests/test_orchestrator.py`
- ملاحظة: `main.py` لا يُلمس — الـ seam قيمة افتراضية حقيقية في constructor الـ orchestrator (نمط `session_timeout_s`).

**⚠️ تحذير سقف الأسطر (محسوم):** `orchestrator.py` عند 299 سطرًا فعليًا. القرار المعتمد: استخراج `voice_out.py` أولًا (يحرّر ~25 سطرًا)، ثم إضافات B (~6 أسطر) — تماماً كما فُصل `highlight_gate.py` و`draw_dispatch.py`.

**خارج النطاق:**
- منطق الـ TTS نفسه (`tts.py` وما تحته)
- أي tool schema
- `claude_agent.py` (الحقن عبر seam الـ system prompt الموجود فقط — Law 11)

### B · TASKS
- [ ] **B1 — موديول verbosity مستقل (محدَّث 2026-07-13).** أنشئ `src/muthis/verbosity.py`: صنف `VerbosityController` يملك حالة الإسهاب عبر الأدوار (NORMAL / SHORT / DETAILED / EXACT_N — الأخيرة تحمل N) ويولّد توجيهاً داخلياً عربياً يُلحق برسالة المستخدم للدور (الخيار A — ليس system prompt). pure stdlib، importable in isolation، DI seam بقيمة افتراضية حقيقية.
  - الملف الجديد: `verbosity.py` + `tests/test_verbosity.py`
  - قبول: اختبار وحدة (كل حالة تنتج توجيهاً مختلفاً؛ NORMAL بلا توجيه؛ الإلحاق يحفظ النص الأصلي)؛ ≤300 سطر.
- [ ] **B2 — كشف أمر الإسهاب من كلام المستخدم (متسامح مع STT — محدَّث 2026-07-13).** في `verbosity.py`: عبارات مثل "اختصر"، "بالتفصيل"، "بخمس كلمات"، "أطول" → تضبط الحالة. normalization قبل المطابقة: توحيد الهمزات ا/أ/إ/آ، إزالة التشكيل، **إزالة الـ tatweel (ـ)** (مثال القبول "بالتفصيــل" يتطلبها)، توحيد المسافات، وتوحيد الأرقام الهندية/العربية. **قاعدة العزل المعتمدة (منع الإيجابيات الكاذبة):** الكلمات المفردة الملتبسة ("اختصر"، "أطول") تُطابق فقط كنطق شبه-منفرد؛ العبارات الصريحة ("بالتفصيل"، "باختصار"، "بـN كلمات") تُطابق في أي موضع — «أي ضلع أطول؟» لا يغيّر الحالة. EXACT_N يفهم أرقامًا وكلمات عدد (١–١٠ + «بكلمتين»). (كشف نصي على كلام المستخدم، ليس coordinate parsing من prose النموذج — مسموح.)
  - قبول: اختبار يؤكد مطابقة الصيغ المتفاوتة ("اختصر"/"إختصر"، "بالتفصيل"/"بالتفصيــل"، "بخمس كلمات"/"ب٥ كلمات") **واختبار سلبي** يؤكد أن "أي ضلع أطول؟" لا يغيّر الحالة.
- [ ] **B3 — ربط الحالة بالدور (مُعاد صياغتها — الخيار A المعتمد 2026-07-13).** الافتراض الأصلي كان خاطئًا: الـ system prompt مجمّد في constructor الـ ClaudeAgent (`main.py` يبنيه مرة واحدة) ولا يصل إليه الـ orchestrator إطلاقًا. البديل المعتمد: `verbosity.py` يولّد توجيهًا داخليًا عربيًا («(توجيه داخلي…») يُلحقه الـ orchestrator بنص المستخدم مرة واحدة عند مطلع الدور (في `run_turn` — لا يُعاد إلحاقه بتمريرات الحلقة الوكيلية الفارغة ولا بـ follow-up الـ refresh)، + سطر طاعة واحد في `persona.py` يأمر النموذج بتنفيذ التوجيهات الداخلية دون قراءتها بصوت عالٍ. `claude_agent.py` و`protocol.py` لا يُلمسان.
  - قبول: اختبار في `test_orchestrator.py` (بـ FakeReasoner يلتقط وسائط run()) يؤكد أن "بخمس كلمات" يوصل توجيه EXACT_N داخل محتوى رسالة المستخدم مع بقاء النص الأصلي؛ اختبار في `test_persona.py` يؤكد سطر الطاعة؛ الحارس ≥279 أخضر.
  - **ملاحظة صدق (شرط قبول واقعي):** النموذج لا يعدّ الكلمات بدقة؛ EXACT_N توجيه تقريبي يقرّب الالتزام ولا يضمنه. القبول = وصول التوجيه الصحيح للـ prompt، وليس أن يكون الرد N كلمة بالضبط. لا تكتب اختباراً يعدّ كلمات ردّ حقيقي.
- [ ] **B4 — دوام الحالة وحدودها (قرار 2026-07-13).** المعتمد: EXACT_N = دور واحد ثم NORMAL؛ SHORT و**DETAILED كلاهما لاصق** حتى عبارة الرجوع («رجّع طولك الطبيعي» وقريباتها) أو أمر جديد يستبدله. **وحدة «الدور» = النطق الكامل** (كل تمريرات الحلقة الوكيلية point→explain) — الـ decay في نهاية `run_turn` بجوار `strip_images_from_history`، وإلا اختفت EXACT_N قبل تمريرة الشرح tool_choice="none". الحالة in-memory وتعود NORMAL مع كل إقلاع.
  - قبول: اختبارات على مستوى الـ orchestrator: توجيه EXACT_N يظهر في دوره ويختفي من الدور التالي؛ SHORT وDETAILED يدومان عبر دورين؛ عبارة الرجوع تعيد NORMAL.

### B · DEFINITION OF DONE
verbosity ديناميكي يعمل · `orchestrator.py` بقي ≤300 سطر (استُخرج `voice_out.py`) · الحارس ≥279 أخضر · LOOK-only محفوظ · تزامن Option A لم يُمسّ · `claude_agent.py`/`protocol.py`/`main.py` لم تُلمس.

---

## المرحلة C — بثّ صوتي أسرع (المخاطرة: عالية · حسّاس · آخر شيء)

### C · SCOPE
**داخل النطاق:**
- `src/muthis/orchestrator.py` — منطق buffer-then-speak → sentence-streaming (عبر موديول مستخرج)
- `src/muthis/tts.py` — للتحقق (C0) فقط، وقراءة الـ seam — لا إعادة هندسة
- `tests/test_orchestrator.py`, `tests/test_orchestrator_tts.py`

**⚠️ الخطر الحقيقي (١) — التزامن:** هذا يمس **تزامن الصوت↔المؤشر** (Option A في AGENTS.md: "الصوت يبدأ مع الرسم"، والـ auto-hide timer يبدأ من لحظة الرسم). أي تغيير في توقيت النطق قد يكسر تزامن الانزلاق، توقيت الـ auto-hide، وانضباط point-then-explain.

**⚠️ الخطر الحقيقي (٢) — تكرار فشل Batch 3:** إن مرّت كل جملة باتصال TTS مستقل (connect-per-sentence) رجعت فجوات التقطيع المسموعة حرفياً. البثّ ينجح فقط عبر اتصال WS واحد دائم يستقبل الجمل تباعاً.

**خارج النطاق:**
- تزامن المؤشر نفسه — يجب أن يبقى كما هو تماماً بعد التغيير
- LOOK-only، tool schemas، الـ agentic loop
- إعادة هندسة `tts.py` (لو C0 كشف الحاجة لها → أوقف وسجّل قرار، لا تنفّذ)

### C · TASKS
- [ ] **C0 — Pre-flight تحقق (بوابة المرحلة كلها — لا كود قبله).** اقرأ مسار الـ TTS الحالي وأجب كتابةً في SESSION LOG: (١) هل `speak()` الحالي يفتح اتصال WS جديداً لكل نداء، أم يمكنه استقبال جمل متتابعة عبر اتصال واحد دائم؟ (٢) ما الـ seam الأنظف لتمرير جمل تباعاً دون إعادة هندسة؟ **قرار البوابة:** إن كان الاتصال-لكل-نداء هو الواقع ولا seam بسيط لتدفق متتابع → المرحلة C تتوقف هنا، سجّل في DECISIONS FOR SULTAN ("C يتطلب تعديل tts.py — يحتاج قرارك")، وانتقل. لا تنفّذ C1–C3 فوق أساس يعيد فجوات Batch 3.
  - قبول: إجابة موثّقة في SESSION LOG + قرار بوابة صريح (متابعة / توقف).
- [ ] **C1 — موديول sentence-splitter مستقل.** أنشئ `src/muthis/speech_stream.py`: يستقبل `TextDelta` fragments ويطلق جملة كاملة عند حدود الجملة العربية (`.` `؟` `!` `؛` والسطر الجديد)، مع حدّ أقصى احترازي (~200 حرف بلا فاصل → flush) كي لا تُحتجز جملة عربية طويلة بلا ترقيم. pure stdlib، importable in isolation، لا يمس التزامن.
  - الملف الجديد: `speech_stream.py`
  - قبول: اختبار وحدة — تدفّق fragments ينتج جملاً صحيحة بالترتيب؛ لا جملة تُقطع في المنتصف؛ الحد الاحترازي يعمل.
- [ ] **C2 — دمج حذر في الـ orchestrator خلف علم.** بدّل buffer-then-speak إلى sentence-streaming **حصراً للدور الوكيلي الخالي تماماً من أي tool_use من أول تمريرة لآخرها** (رد نصي صرف). **القيد الحاسم المعدَّل:** أي دور فيه أي tool_use (رسم/refresh) — بكل تمريراته، **بما فيها تمريرة الشرح tool_choice="none" بعد الرسم** — يبقى buffer-then-speak كاملاً. السبب: تمريرة الشرح لا ترسم لكنها تعيش تحت auto-hide timer بدأ من لحظة الرسم؛ بثّها يغيّر مدة النطق الفعلية فيختفي المستطيل قبل اكتمال الشرح (كسر صامت لا يمسكه اختبار تزامن حالي). خلف علم `.env` (مثل `MUTHIS_STREAM_TTS=1`) قابل للإطفاء الفوري.
  - قبول: اختبار يؤكد أن دوراً فيه highlight (بكل تمريراته، الشرح ضمناً) يبقى buffer-then-speak، وأن دوراً نصياً صرفاً يبثّ جملاً بالترتيب؛ العلم يطفئ الميزة كلياً.
- [ ] **C3 — تحقق التزامن لم يُكسر.** شغّل كل اختبارات التزامن والـ auto-hide والـ agentic loop.
  - قبول: `test_overlay_autohide.py`, `test_orchestrator.py` (كل اختبارات الحلقة والتزامن) خضراء بالكامل.

### C · DEFINITION OF DONE
C0 موثّق وقراره صريح · البثّ أسرع للردود النصية الصرفة فقط · أي دور فيه tool_use بقي buffer بكل تمريراته · العلم يطفئ الميزة · الـ278 خضراء · Option A محفوظ.

---

## المرحلة D — دقة التأشير (اختيارية · قياسية · المخاطرة: منخفضة-متوسطة)

> أضيفت في المراجعة: الهدف المعلن "تحسين طريقة التأشير" لم يكن مغطّى. أضعف نقطة عملية لوحظت في v4.1 هي زحلقة الأشكال على التفاصيل الدقيقة. هذه المرحلة قياسية: قِس قبل/بعد وقرّر — لا التزام مسبق.

### D · SCOPE
**داخل النطاق:**
- `.env` / `src/muthis/vision/downscale.py` — قيمة `MUTHIS_VISION_MAX_WIDTH` (حالياً 1280)
- `src/muthis/persona.py` — سطر توجيه تأشيري واحد
- **خارج النطاق:** أي تعديل على scaling math نفسه، أي tool schema، الـ agentic loop.

### D · TASKS
- [ ] **D1 — قياس أساس.** وثّق في SESSION LOG دقة التأشير الحالية على 3 حالات مرجعية (زر واجهة، أيقونة صغيرة، عنصر داخل صورة) بلقطات قبل/بعد.
- [ ] **D2 — تجربة دقة أعلى.** ارفع `MUTHIS_VISION_MAX_WIDTH` إلى 1600 (عبر `.env` فقط — الكود يدعمها أصلاً). أعد القياس على نفس الحالات. سجّل الفرق في الدقة **وفرق تكلفة tokens/التأخير** (الصورة الأكبر أغلى وأبطأ).
- [ ] **D3 — توجيه persona تأشيري.** أضف سطراً واحداً للـ persona: "صوّب الإحداثيات إلى مركز العنصر المستهدف، لا حافته". أعد القياس.
  - قبول للمرحلة كلها: جدول قبل/بعد في SESSION LOG (دقة، تكلفة، تأخير) + توصية صريحة (اعتماد 1600 أو البقاء على 1280) في DECISIONS FOR SULTAN. لا اعتماد صامت.

### D · DEFINITION OF DONE
قياسات موثّقة · توصية صريحة تنتظر قرار سلطان · لا تغيير معتمد بلا قياس · الـ278 خضراء.

---

## SELF-VERIFICATION LOOP (حبل الأمان — إلزامي بعد كل مهمة)

1. شغّل: `pytest -q`
2. إذا فشل أي اختبار → صحّح قبل الانتقال. لا تتقدّم فوق اختبار أحمر.
3. تأكد أن العدد بقي 278 passed (أو أعلى مع اختبارات جديدة). أي نقص = انحدار، أوقف.
4. إذا كسرتَ اختباراً قديماً ولم تعرف السبب → تراجع، سجّل في SESSION LOG، انتقل للمهمة التالية.
5. commit بعد كل مهمة ناجحة (Conventional Commit).

## GUARDRAILS (خطوط حمراء)
- لا تعدّل أي ملف خارج SCOPE للمرحلة الحالية.
- لا تحذف اختباراً لتمريره. الأحمر يُصلَّح.
- لا تتجاوز 300 سطر في أي ملف — استخرج موديولاً بدلاً من ذلك.
- لا تخمّن عند غموض معماري — سجّله في DECISIONS FOR SULTAN وتخطّاه.
- المرحلة A: لا عنصر جديد حول المؤشر (لا عودة للهالة بأي اسم).
- المرحلة C: C0 بوابة إلزامية؛ أي دور فيه tool_use = buffer بكل تمريراته؛ عند الشك، توقف واسأل.
- المرحلة D: قياس قبل أي اعتماد؛ التوصية لسلطان لا اعتماد صامت.

## DECISIONS FOR SULTAN (قرارات تنتظرني)
<!-- Claude Code يكتب هنا أي قرار علّق عنده بدل إيقاف الجلسة. أراجعه لما أرجع. -->

### محسومة (سلطان — 2026-07-13، مراجعة تصميم المرحلة A)
1. **A1 ملغاة (الخيار ج):** تلوين المؤشر المنزلق حسب الحالة لا يبرّر توسيع النطاق إلى `sidekick_window.py` ولا مخاطرة السلك قرب مسار العرض؛ النقطة الركنية كافية لتمثيل الحالة.
2. **استخراج `overlay/style_env.py` معتمد:** فصل env-parsing من `style.py` (كان عند 300 سطر بالضبط — السقف) قبل A3، مع تحديث جدول Key Files في `AGENTS.md`.
3. **مقياس A2 معتمد:** دورة النبض 2000ms (بدل 1400) وسعة 2px (بدل 3).
4. **مقياس A3 معتمد:** خفض `glow_intensity` الافتراضي 0.45 → 0.35.

5. **مقياس caption معتمد (لاحقًا في 2026-07-13):** رفع `label_font_size` الافتراضي 14 → 16 — أُقفل بذلك آخر جزء معلّق من A3.

### محسومة (سلطان — 2026-07-13، مراجعة تصميم المرحلة B)
6. **آلية الحقن — الخيار A:** التوجيه الداخلي يُلحق برسالة المستخدم من الـ orchestrator (المالك الشرعي للنص والـ history)؛ الـ system prompt يبقى مجمّدًا كما وثّقته persona.py؛ `claude_agent.py`/`protocol.py` لا يُلمسان؛ سطر طاعة واحد في `persona.py`.
7. **استخراج `voice_out.py` معتمد:** نقل `_speak` + `_refuse_for_budget` (حدّ الخصوصية + رقصة ضوء الحالة + الرفض المنطوق) قبل إضافات B، مع تحديث جدول `AGENTS.md`.
8. **DETAILED لاصق مثل SHORT:** يدوم حتى أمر جديد أو عبارة الرجوع؛ EXACT_N وحدها one-shot.
9. **الأوامر الخالصة تمرّ لـ Claude (v1):** «اختصر» وحدها = دورة كاملة مع التوجيه؛ المعالجة المحلية بلا نداء تحسين مؤجَّل.
10. **قاعدة العزل للكلمات الملتبسة:** «أطول»/«اختصر» المفردة تطابق كنطق شبه-منفرد فقط — «أي ضلع أطول؟» لا يغيّر الحالة.
11. **مصادقات شكلية:** `test_verbosity.py` جديد، سطرا `verbosity.py`/`voice_out.py` في جدول Key Files، إعادة صياغة B3/B4 في الخطة.

### معلّقة (تنتظر قرار سلطان)
- لا شيء حاليًا.

## SESSION LOG (يكتبه Claude Code أثناء العمل)
<!-- كل مهمة، كل اختبار كُسر وأُصلح، كل موديول استُخرج، كل تراجع. -->

- 2026-07-13 · خط الأساس مؤكَّد قبل أي تغيير: `pytest -q` → **278 passed, 1 skipped** على `.venv-v5` (Python 3.14.4)، فرع `mut-his-v5-experimental`.
- 2026-07-13 · توثيق قرارات مراجعة المرحلة A (إلغاء A1، اعتماد `style_env.py`، مقاييس A2/A3) + إعادة تسمية الملف من `plan_v5 (1).md` إلى `plan_v5.md`.
- 2026-07-13 · **الاستخراج:** أُنشئ `overlay/style_env.py` (117 سطرًا — env-parsing + حارس المفتاح الشفاف، `TRANSPARENT_KEY` انتقل إليه ويُعاد تصديره من `style.py`)؛ `style.py` انخفض 300 → 223 سطرًا. جدول Key Files في `AGENTS.md` حُدِّث. `pytest -q` → **278 passed, 1 skipped** ✅ (لا انحدار).
- 2026-07-13 · **A2 منجزة:** `_PULSE_PERIOD_MS` 1400 → 2000، `DOT_PULSE_AMP` 3 → 2 في `status_indicator.py`. اختبار كمّي جديد (`test_pulse_breath_is_gentle_2000ms_period_2px_amplitude`) يثبت الذروة 7+2px عند منتصف الدورة، إغلاق الدورة بعد 40 tick، وفرق الإطارين المتتاليين < 0.2px (الإعداد القديم كان يفشل به عند ~0.34px). لا blocking sleep — حلقة الـ schedule كما هي. `pytest -q` → **279 passed, 1 skipped** ✅.
- 2026-07-13 · **A3 منجزة:** `glow_intensity` الافتراضي 0.45 → 0.35 في `style.py` (dataclass + fallback الـ from_env). **تحديث اختبار موثَّق (ليس إسكاتًا):** قيمتا 0.45 المثبَّتتان حرفيًا في `test_overlay_style.py` (سطرا defaults وglow_strokes) حُدِّثتا إلى 0.35 لمطابقة الـ default الجديد المعتمد؛ بقية الاختبارات تقرأ `DEFAULTS.glow_intensity` ديناميكيًا فتكيّفت بلا تعديل. سطر توثيق `MUTHIS_GLOW_INTENSITY` في `AGENTS.md` حُدِّث. جزء caption معلّق في DECISIONS. `pytest -q` → **279 passed, 1 skipped** ✅.
- 2026-07-13 · **المرحلة A مكتملة** (A1 ملغاة، الاستخراج + A2 + A3 منجزة): 4 commits، الحارس عند 279 (فوق خط الأساس 278)، لا ملف تجاوز 300 سطر، LOOK-only لم يُمسّ، تزامن Option A لم يُلمس.
- 2026-07-13 · **إقفال A3 نهائيًا (قرار caption المعتمد):** `label_font_size` الافتراضي 14 → 16 في `style.py` (dataclass + fallback)، تحديث موثَّق للتثبيت في `test_overlay_style.py` وسطر `MUTHIS_LABEL_SIZE` في `AGENTS.md`. `pytest -q` → **279 passed, 1 skipped** ✅.
- 2026-07-13 · **بوابة المرحلة B فُتحت بعد المراجعة التصميمية:** كُشف أن افتراض B3 الأصلي (حقن عبر resolve_system_prompt لكل دور) مستحيل — الـ system prompt مجمّد في constructor الـ ClaudeAgent بقرار موثَّق في persona.py. اعتُمد الخيار A (توجيه داخلي في رسالة المستخدم) + استخراج `voice_out.py` + قواعد الكشف والدوام. B1–B4 أُعيدت صياغتها بالقرارات 6–11.
